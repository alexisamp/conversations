{
  "name": "conversations",
  "version": "0.0.1",
  "private": true,
  "description": "WhatsApp client con sidebar de RedThink — fase 0",
  "main": "dist/electron/main.js",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json",
    "start": "npm run build:main && electron .",
    "dev": "npm run build:main && electron .",
    "dev:devtools": "npm run build:main && CONV_DEVTOOLS=1 electron .",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "electron": "^33.2.0",
    "typescript": "^5.6.3"
  }
}
